# DELETE-Removing-a-Record-from-a-File
C++ file streams cannot directly remove a line from the middle of a text file, because the operating system treats a text file as a continuous byte stream, not as a list of independent lines. The standard approach is the temporary-file technique: read the original file record by record.....
